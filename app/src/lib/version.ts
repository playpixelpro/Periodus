/**
 * Bundled app version — must match the `versionName` in
 * `app/android/app/build.gradle` and the latest GitHub release tag.
 * Bump this (and the Gradle versionCode/versionName) before tagging a release.
 */
export const APP_VERSION = '1.0.0'

/** GitHub repository that hosts the releases. */
export const GITHUB_REPO = 'playpixelpro/Periodus'
